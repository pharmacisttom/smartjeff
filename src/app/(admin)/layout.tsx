import { ApplicationShell } from "@/components/layout/ApplicationShell";
export default function OperationsLayout({ children }: { children: React.ReactNode }) { return <ApplicationShell>{children}</ApplicationShell>; }
