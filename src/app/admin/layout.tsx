import { ApplicationShell } from "@/components/layout/ApplicationShell";
export default function AdminLayout({ children }: { children: React.ReactNode }) { return <ApplicationShell>{children}</ApplicationShell>; }
